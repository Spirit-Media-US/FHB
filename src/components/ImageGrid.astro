---
// ImageGrid — scaffold (Phase 2)
// Phase 3: Add cols prop, image type, captions, R2/Sanity URLs
interface Props {
	cols?: number;
}
const { cols = 3 } = Astro.props;
---
<div class="image-grid">
  <slot />
</div>
