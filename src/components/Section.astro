---
// Section — scaffold (Phase 2)
// Phase 3: Add variants (light/dark), padding, max-width props
interface Props {
	id?: string;
}
const { id } = Astro.props;
---
<section id={id}>
  <slot />
</section>
