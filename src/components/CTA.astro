---
// CTA — scaffold (Phase 2)
// Phase 3: Add href, label, variant (primary/secondary) props
interface Props {
	href: string;
	label: string;
}
const { href, label } = Astro.props;
---
<a href={href}>{label}</a>
