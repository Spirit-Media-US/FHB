---
// /samples — retired. The sample-passage page collapsed into the live reader;
// the Father's Heart Bible is now read directly at /read (free, with audio).
// Preserved as a 301 redirect for any cached links or social shares.
export const prerender = true;
return Astro.redirect('/read', 301);
---
